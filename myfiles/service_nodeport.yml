apiversion: v1
kind: service
metadata:
  name: nodeport

spec:
  type: NodePort
  ports:
    - targetPort: 80
      port: 80
      nodeport: 30008
  selector:
    author: Bharath
    type: reverse-proxy

---

